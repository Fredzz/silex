<!doctype html>
<html>
    <head>
 {% block stylesheets %}
            <link href="{{ app.request.basepath }}/css/main.css" type="text/css" rel="stylesheet" />
  {% endblock %}
 {% block js %}

  {% endblock %}

            <title>{% block title %}Homepage{% endblock %}</title>
        </head>
        <body>
            <div class="menu">
                <nav>
                    <ul>
                        <li><a href="{{ app.url_generator.generate('home') }}">Home</a></li>
                        <li><a href="{{ app.url_generator.generate('authors') }}">Authors</a></li>
                        <li><a href="{{ app.url_generator.generate('post_new') }}">Add Post</a></li>                        
                    </ul>
                </nav>
            </div>
            <div class="content">
           {% block content %}
                    <h2>Welcome to the Silex test by Fred Duarte</h2>
                    <p>Please use the menu above to navigate through the site.</p>
                {% endblock %}
                </div>
                <footer>
                </footer>
            </body>
        </html>
